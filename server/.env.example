# Django Settings
SECRET_KEY=your-secret-key-here-generate-new-one
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# Database - Use SQLite for development (set to True) or PostgreSQL (set to False)
USE_SQLITE=True

# Database
DB_ENGINE=django.db.backends.postgresql
DB_NAME=flypick_db
DB_USER=flypick
DB_PASSWORD=idahamsm@
DB_HOST=localhost
DB_PORT=5432

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080,http://localhost:8081,http://localhost:3000,https://yourdomain.com

# JWT Settings
JWT_ACCESS_TOKEN_LIFETIME=15
JWT_REFRESH_TOKEN_LIFETIME=1440

# Security
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=True
SMTP_USER=boibazar00@gmail.com
SMTP_PASS= 

# Email Sender Configuration
EMAIL_SENDER_NAME=Flypick

# Site Configuration
SITE_NAME=Flypick
FRONTEND_URL=http://localhost:8080
SELLER_FRONTEND_URL=http://localhost:8081
